# GOAI 2026 初赛提交草案：Opportunity Memory Agent

## 参赛方向

Boundless Agents（无界智能体）——面向跨周、跨平台的真实机会管理场景。

## 一句话说明

一个具备可审计长期记忆的机会推进智能体：它把截止时间、审核回复、阻塞证据和下一步操作写入持久化事件流，并生成有事实依据的优先行动队列。

## 要解决的问题

竞赛、开源赏金和商业机会往往持续数周。普通对话机器人容易忘记旧回复、重复报名、混淆模拟成绩与真实成绩，或在法律边界前误操作。Opportunity Memory Agent 将每次变化记录为不可覆盖的事件，再根据截止时间、状态和置信度计算下一步，从而让过程可复核、可恢复、可协作。

## 开源与复现

- MIT 许可证，公开源码、测试和容器配置。
- `docker compose up --build` 启动 API 与 CockroachDB。
- `python scripts/seed_demo.py` 写入可演示数据。
- 无 Gemini 密钥时，持久化、时间线、排序和仪表盘仍可运行；密钥只用于对前十条已排序事实生成摘要。
- 十项自动化测试覆盖页面、排序、防幻觉提示构造和零凭据真实界面演示。

## 核心创新

1. **事件溯源式记忆**：审核、进展和阻塞作为追加事件保存，不覆盖历史。
2. **证据优先排序**：综合截止压力、状态与置信度，不让高奖金但不可执行的事项挤占队列。
3. **受约束的生成层**：模型只读取已排序的结构化事实，并被明确禁止虚构资格、进展和奖励。
4. **人机边界显式化**：身份、条款、付款和验证码可记录为阻塞，而不是被自动化流程偷偷越过。
5. **轻量可部署**：无前端构建链，单容器 API 可部署至 AWS App Runner，数据库使用 CockroachDB。

## 评审演示（约 3 分钟）

1. 创建一个带奖金、截止时间和置信度的机会。
2. 添加“主办方审核中”事件，展示完整持久时间线。
3. 再创建一个奖金较低但临近截止且可执行的机会。
4. 打开行动队列，说明为何后者排在前面。
5. 请求 Gemini brief，展示摘要严格引用数据库事实。
6. 重启 API，证明时间线和排序仍然存在。

## 当前可核验证据

- 公开仓库：`https://github.com/ILoveBuns/opportunity-memory-agent`
- 本地测试：10 项通过；打包脚本会在生成 ZIP 前强制重跑
- 部署配置：`Dockerfile`、`docker-compose.yml`、`apprunner.yaml`
- 操作说明：`README.md`、`DEPLOYMENT.md`
- 提交证据及 PDF/视频校验值：`GOAI_EVIDENCE.md`

## 平台边界与当前状态

- 不宣称已产生收入、客户或云端运行实例。
- 正式报名和初赛第一次提交已经完成，平台记录为审核中。
- 主办方因平台 ZIP 存储损坏问题要求重新上传原始附件；当前仍有两次提交机会。
- 收件信息、实名、协议和收款资料须由参赛者本人确认。
- 重传步骤及成功判据见 `GOAI_REUPLOAD_GUIDE.md`。
